import { ReferenceStepProps } from "../ReferenceForm";

const FeltUnsafeStep = ({
  referenceData,
  setReferenceValues,
  referenceType,
  hostRequestId,
}: ReferenceStepProps) => {
  return <>FELT UNSAFE</>;
};

export default FeltUnsafeStep;
